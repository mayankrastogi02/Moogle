export default interface SearchResults {
    content: string | null,
    created_at: string,
    desc: string | null,
    id: number,
    logo: string | null,
    title: string | null,
    url: string | null,
    name: string | null,
}